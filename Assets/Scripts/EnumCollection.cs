using UnityEngine;
using System.Collections;


public enum PlayerAction { unset, wait, move, attack, ability, interact };

public enum EnemyAction { unset, wait, move, attack }

public enum Direction { forward, right, back, left };

